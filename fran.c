#include <stdio.h>
#include <string.h>
#include <unistd.h>

int main() {
    char buf[20];
    int index = 4;
    buf[0] = a;
    buf[1] = b;
    buf[2] = c;
    buf[3] = d;
    buf[4] = e;

    printf("Buffer position number %i corresponds to: %c",index,buf[index]);

    return 0;

}